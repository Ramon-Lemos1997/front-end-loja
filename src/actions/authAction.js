//para o reducers;
/*export const loginSuccess = (user, expirationDate) => {
    return{
        type: "LOGIN_SUCCESS",
        payload: { user, expirationDate },
    };
};
export const logout = () => {
    return{
        type: "LOGOUT",
    };
};*/
//adsa